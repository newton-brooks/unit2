function TotalRadSystemHTML(htmlString,elementOpen, ElenmentClose, newText){
    let words = htmlString.split("")
    let between = false;
    let amswer = "";

    words.function((word) => {
        if(between === false) {
            answer = answer.concat(word + " ")
        } (word === elementOpen) {
            between =
        }
    })

}